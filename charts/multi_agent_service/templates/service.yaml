apiVersion: v1
kind: Service
metadata:
  name: {{ include "multi-agent-service.fullname" . }}
  labels:
    {{- include "multi-agent-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "multi-agent-service.selectorLabels" . | nindent 4 }}
