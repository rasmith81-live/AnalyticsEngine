import { Typography, Box } from '@mui/material';

export default function ConfigPage() {
  return (
    <Box>
      <Typography variant="h4">
        Config Page - Test Version
      </Typography>
      <Typography variant="body1">
        If you see this, the basic page works. The issue is in MetricTreeTabs.
      </Typography>
    </Box>
  );
}
